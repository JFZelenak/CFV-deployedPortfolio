<form method="post" action="{{ path('app_art_items_delete', {'id': art_item.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');" class="d-inline">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ art_item.id) }}">
    <button class="border border-dark link-dark text-decoration-none link-opacity-75 link-opacity-100-hover shadow bg-light" style="padding: 3px;">Delete</button>
</form>
